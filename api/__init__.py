"""
api

FastAPI layer exposing Ariadne's core modules over HTTP: the same
fetch, propagate, and conjunction machinery the CLI drives, wrapped
for the dashboard instead of argv. Additive to `ariadne/`, nothing
here changes library internals.
"""
